{
  "name": "lane",
  "description": "Agent-native payments in Claude Code. Drives Lane's deterministic purchase flow — draft a purchase intent, approve it with a passkey, then let checkout-mcp autonomously complete the buy — over the hosted intent-mcp and checkout-mcp servers. Auto-installs the Lane CLI.",
  "author": {
    "name": "Lane",
    "email": "agentic@getonlane.com",
    "url": "https://getonlane.com"
  },
  "homepage": "https://getonlane.com/",
  "repository": "https://github.com/Lane-Technologies-Inc/lane-plugin",
  "license": "MIT",
  "keywords": [
    "payments",
    "wallet",
    "checkout",
    "credentials",
    "visa",
    "agentic-commerce",
    "mcp"
  ],
  "userConfig": {
    "lane_api_key": {
      "type": "string",
      "title": "Lane API key",
      "description": "Authenticates to the intent-mcp and checkout-mcp servers. Leave this BLANK at install — the Lane account-setup flow auto-fills it after you run `lane-cli init` (it copies the apiKey from ~/.lane/config.json so the MCP servers use the same account as your CLI). Only set it by hand if you want to point the plugin at a different account than your CLI login.",
      "required": false
    }
  },
  "mcpServers": {
    "intent-mcp": {
      "type": "http",
      "url": "https://mcp-staging.aws.getonlane.com/intent/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.lane_api_key}"
      }
    },
    "checkout-mcp": {
      "type": "http",
      "url": "https://mcp-staging.aws.getonlane.com/checkout/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.lane_api_key}"
      }
    }
  }
}
